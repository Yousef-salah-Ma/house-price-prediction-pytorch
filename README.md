# house-price-prediction-pytorch
An end-to-end Deep Neural Network pipeline built with PyTorch to predict house prices. Features robust preprocessing (One-Hot Encoding, StandardScaler), Batch Normalization, and Dropout regularization to achieve optimal regression performance
